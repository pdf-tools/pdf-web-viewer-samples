# Localization

This is a simple example that demonstrates the basic usage of the PDF Web Viewer library, with a focus on the localization.

In this example, we configure the Viewer to use the French locale and add a new language, Spanish.
Then, we are using the available API methods to get available locales and to change the locale to the newly added language.
